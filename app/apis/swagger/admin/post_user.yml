Cria um usuário
---
tags:
  - Usuários
definitions:
  UserCreate:
    type: object
    properties:
      nome:
        type: string
      email:
        type: string
      senha:
        type: string
      ocupacao:
        type: string
      empresa_id:
        type: integer
      valor:
        type: integer
parameters:
  - name: body
    in: body
    required: true
    schema:
      $ref: "#/definitions/UserCreate"
responses:
  201:
    description: Cria um usuário
    examples:
      application/json: { "id": 1 }
